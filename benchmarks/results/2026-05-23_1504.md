# CogniGraph benchmark — 2026-05-23

**Model**: `mock` · **Dataset**: `synthetic-fallback` · **Wall-clock duration**: 0.0 min

## Workload

- 30 measurement queries + 10 warm-up queries
- 8 intents in the source dataset
- Top 5 intents cover 93.3% of measurement queries (head-heavy Zipfian distribution)
- ⚠️ This run used the **synthetic fallback dataset** — the numbers are illustrative, not from a real corpus.

## Headline numbers

| System | Deflection | Cost / 1k queries | p50 latency | p95 latency | p99 latency | Errors |
|---|---:|---:|---:|---:|---:|---:|
| `mock_llm` | 0.0% | $0.59 | 11.2ms | 18.1ms | 18.5ms | 0 |

## Route breakdown

| System | graph_hit | cache_hit | llm_call | error |
|---|---:|---:|---:|---:|
| `mock_llm` | 0 | 0 | 30 | 0 |

## Token + cost detail

| System | Total input tokens | Total output tokens | Total cost | Cost / query |
|---|---:|---:|---:|---:|
| `mock_llm` | 1,524 | 884 | $0.02 | $0.06¢ |

## Methodology

- Each system runs against the **same query stream** (same dataset, same Zipfian seed, same model). Cache / graph systems also see a warm-up phase; raw LLM does not (it's stateless, warm-up would only waste tokens).
- Latency is wall-clock from query in → response out, including all in-process work (normalize, embed, match, safety, LLM call when applicable).
- Token counts come from the Anthropic SDK's `usage` field. Cost is derived from published pricing at run time — see `benchmarks/runner.py::PRICING`.
- `deflection` = share of queries that did NOT hit the LLM. For `raw_llm` this is structurally 0%.
- Quality is not auto-scored in this run; spot-check the per-query records in the accompanying JSON file to verify responses are sensible. A future iteration could add cosine similarity vs canonical responses or a judge-LLM rating.

## Caveats

- Single workload, single dataset, single model. Real workloads vary. The pattern is more important than the absolute numbers.
- CogniGraph's `learning_starting_confidence` (0.5) is below the `confidence_threshold` (0.7), so newly-crystallized nodes first route LLM_FALLBACK and only graduate to GRAPH_DIRECT after additional reinforcement. With a longer warm-up and / or tuned thresholds, deflection improves materially.
- No GPTCache comparison in this run. The wrapper is on the roadmap; today the headline is CogniGraph vs raw LLM.
